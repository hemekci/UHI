# tests/

Pytest suite covering `pipeline/` modules.

Run locally:

```bash
uv run pytest -q
```

CI runs the full suite on every push and pull request — see
`.github/workflows/ci.yml`.

To be populated alongside `pipeline/` after manuscript acceptance.
